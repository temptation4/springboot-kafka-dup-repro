# Kafka Duplicate Message Repro (Spring Boot + Toxiproxy)

See README steps from canvas — run docker-compose, create topic, set up toxiproxy, and run Spring Boot app. 
Cut ACKs using toxiproxy to observe duplicates.
